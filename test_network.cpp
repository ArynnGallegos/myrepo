
#include "network.h"

int main(){

    Network myNet;
    //Connection* c1 = new Connection("connection_template.txt");
    myNet.showMenu(); 
    /*cout<<"[Empty network:] "<<endl;
    myNet.printDB();
    myNet.push_front(c1);
    cout<<endl<<"[After adding first contact:] " << endl;
    myNet.printDB();
    
    myNet.loadDB("studentDB.db");
    cout << endl << "[After loading networkDB:] " << endl;
    myNet.printDB();
    
    myNet.saveDB("outputtest.txt");*/

    return 0;
} 
